<%- include("partials/header") %>

<h1><%= campgound.name %></h1>

<img src="<%= campgound.image %>" alt="">

<p><%= campgound.description %></p>


<% campgound.comments.forEach(function(comment){ %>
    <p>
        <%= comment.text%>

      <strong><%= comment.author %></strong>
    </p>

<% }); %>

<%- include("partials/footer") %>